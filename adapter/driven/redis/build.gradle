plugins {
    id 'io.spring.dependency-management' version '1.1.0'
    id 'java-library'
}

version '1.0.0'

dependencies {
    compileOnly 'org.springframework.boot:spring-boot-starter-data-redis:3.1.0'

    compileOnly project(":application")
    compileOnly project(":commons:mapper-common")
}